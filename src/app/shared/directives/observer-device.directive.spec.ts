import { ObserverDeviceDirective } from './observer-device.directive';

describe('ObserverDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserverDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
